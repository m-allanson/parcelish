{
  "name": "parcelish",
  "main": "src/index.js",
  "scripts": {
    "bundle": "node scripts/bundle.js",
    "clear": "rm -r .parcel-cache && rm -r dist"
  },
  "dependencies": {
    "parcel": "^2.0.0-beta.1"
  }
}
