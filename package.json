{
  "name": "parcelish",
  "scripts": {
    "bundle": "node scripts/bundle.js",
    "clear": "rm -r .parcel-cache && rm -r dist"
  },
  "dependencies": {
    "pad-left": "^2.1.0",
    "parcel": "^2.0.0-beta.1"
  }
}
